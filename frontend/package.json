{
  "name": "einkaufsliste-frontend",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://ochtii.github.io/einkaufsliste",
  "proxy": "http://ochtii.run.place:4000",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "start": "cross-env HOST=0.0.0.0 DANGEROUSLY_DISABLE_HOST_CHECK=true NODE_OPTIONS=--no-deprecation react-scripts start",
    "build": "cross-env NODE_OPTIONS=--no-deprecation react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
